# Docker Hub image pull secret
# To create this secret, run:
# kubectl create secret docker-registry dockerhub-secret \
#   --docker-server=docker.io \
#   --docker-username=varunwalia \
#   --docker-password=YOUR_DOCKERHUB_PASSWORD \
#   --docker-email=YOUR_EMAIL \
#   -n logwise
#
# Or use a Docker Hub access token (recommended):
# kubectl create secret docker-registry dockerhub-secret \
#   --docker-server=docker.io \
#   --docker-username=varunwalia \
#   --docker-password=YOUR_ACCESS_TOKEN \
#   --docker-email=varunwalia@example.com \
#   -n logwise
#
# Note: This file is a placeholder. The actual secret should be created manually
# or via CI/CD pipeline with proper secret management.


